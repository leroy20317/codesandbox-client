# Roadmap

大型事前规划目录。当需求大到"塞不进单个 feature"时，在这里做概设 + 接口契约 + 子 feature 拆解。

> 使用 `cs-roadmap` 流程创建或更新 roadmap。

## 现有 Roadmap

_暂无。使用 `cs-roadmap` 流程添加。_

## 格式

每个 roadmap 一个子目录：`{slug}/`

```
{slug}/
├── README.md         # 概设 + 接口契约
└── features/         # 子 feature 拆解清单
```
