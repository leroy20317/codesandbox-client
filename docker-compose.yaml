services:
  sandpack-bundler:
    # The image is published to Docker Hub.
    # Runtime defaults to SANDPACK_PACKAGE_RESOLVE_MODE=local-first.
    image: ghcr.io/leroy20317/sandpack-bundler:latest
    container_name: sandpack-bundler
    restart: unless-stopped
    ports:
      - '9801:80'
